{
  "name": "xyops-freeipa-management",
  "version": "2.3.0",
  "private": true,
  "description": "xyOps FreeIPA management, SSH command execution and workflow orchestration plugins",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "xyops-freeipa-management": "runner.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check runner.js && node --check ssh.js"
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "engines": {
    "node": ">=18"
  }
}
