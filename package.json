{
  "name": "xyops-freeipa-management",
  "version": "2.8.0",
  "private": true,
  "description": "xyOps FreeIPA management and SSH local-user lifecycle workflows",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "xyops-freeipa-management": "runner.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check runner.js && node --check ssh.js && node --check ssh-remove-key.js && bash -n scripts/create-local-user.sh && bash -n scripts/remove-user-ssh-key.sh"
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "engines": {
    "node": ">=18"
  }
}
