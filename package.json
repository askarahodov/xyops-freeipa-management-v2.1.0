{
  "name": "xyops-freeipa-management",
  "version": "2.2.2",
  "private": true,
  "description": "xyOps Event Plugins for FreeIPA management with top-level Bucket Menu selectors",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "xyops-freeipa-management": "runner.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check runner.js"
  },
  "engines": {
    "node": ">=18"
  }
}
