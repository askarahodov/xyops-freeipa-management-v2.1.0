{
  "name": "xyops-freeipa-management",
  "version": "2.5.0",
  "private": true,
  "description": "xyOps FreeIPA management and SSH local-user provisioning workflow",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "xyops-freeipa-management": "runner.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check runner.js && node --check ssh.js && bash -n scripts/create-local-user.sh"
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "engines": {
    "node": ">=18"
  }
}
